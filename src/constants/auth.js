/** Password reset email redirect (Supabase Auth). Use Expo Go URL in dev; switch for production. */
export const PASSWORD_RESET_REDIRECT = 'exp://localhost:8081';
// Production (App Store): 'sankofafit://reset-password'
